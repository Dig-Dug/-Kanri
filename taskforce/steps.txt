npm create vue@latest
     Project name → taskforge

                Add TypeScript? (👉 yes, since it’s industry standard)
                Add JSX support? (👉 no, not needed)
                Add Router? (👉 yes)
                Add Pinia? (👉 yes)
                Add Vitest? (👉 yes, good practice)
                Add ESLint/Prettier? (👉 yes, clean code)

                npm install
                    run dev

Create .dockerignore
Create Dockerfile (name it just like that)

test that it works:
    run docker
    docker build -t taskforge-frontend .
    docker run -p 8080:80 taskforge-frontend
    Check 👉 http://localhost:8080

in powershell:
        docker build -t taskforge-frontend .
        docker run -p 8080:80 taskforge-frontend

create folder structure:

        taskforce/
        ├─ backend/
        │  ├─ package.json
        │  ├─ index.js 
           | Dockerfile
        └─ docker-compose.yml
        check in powershell: 
           docker-compose up --build

    cd backend
         npm init -y
         npm install express cors pg

in powershell: 
        docker exec -it taskforce-db-1 psql -U postgres -d taskforce
        CREATE TABLE tasks (
        id SERIAL PRIMARY KEY,
        name VARCHAR(255) NOT NULL
        );
        demo tasks:
        INSERT INTO tasks (name) VALUES
        ('Learn Docker'),
        ('Build Backend API'),
        ('Connect Vue Frontend'),
        ('Test with Postman'),
        ('Celebrate small wins');

    test in postman Get request: http://localhost:3000/api/tasks

DB---create folder db, check sql, 
     docker-compose should be fixed accordingly

Create -->  src/components/TasksList.vue
   mount it in --> src/App.vue




🔹 Step 4 — Add README.md
# TaskForge

TaskForge is a lightweight task dashboard built with **Vue 3**, containerized with **Docker**, and ready for deployment on **Kubernetes**.

## 🚀 Getting Started

### Local Development
```bash
npm install
npm run dev
App runs at: http://localhost:5173 local test
App runs at: http://localhost:8080 production

📦 Tech Stack

Vue 3 (Composition API + <script setup>)

Vite

Pinia

Vue Router

Docker

Nginx

🔮 Next Steps

Add backend (Node + SQL)

Deploy on Kubernetes

CI/CD pipeline